const TYPE_COMMON = [
  // 문자열
  "VARCHAR",          // PG: CHARACTER VARYING  Oracle: VARCHAR(→VARCHAR2)  MariaDB: ✓  MSSQL: ✓
  "CHAR",             // PG: ✓  Oracle: ✓  MariaDB: ✓  MSSQL: ✓
  // 정수
  "INTEGER",          // PG: ✓  Oracle: →NUMBER  MariaDB: →INT  MSSQL: →INT
  "SMALLINT",         // PG: ✓  Oracle: →NUMBER  MariaDB: ✓  MSSQL: ✓
  // 실수·소수
  "DECIMAL",          // PG: →NUMERIC  Oracle: →NUMBER  MariaDB: ✓  MSSQL: ✓
  "NUMERIC",          // PG: ✓  Oracle: →NUMBER  MariaDB: ✓  MSSQL: ✓
  "FLOAT",            // PG: ✓  Oracle: ✓  MariaDB: ✓  MSSQL: ✓
  "REAL",             // PG: →FLOAT4  Oracle: →FLOAT(63)  MariaDB: ✓  MSSQL: →FLOAT(24)
  // 날짜
  "DATE",             // PG: 날짜만  Oracle: 날짜+시간(주의)  MariaDB: 날짜만  MSSQL: 날짜만
] as const;
// ※ 제외된 타입:
//   TEXT      — Oracle에서 DDL 에러 (CLOB 사용)
//   BIGINT    — Oracle에서 DDL 에러 (NUMBER(19) 사용)
//   TIMESTAMP — MSSQL에서 ROWVERSION(바이너리 카운터)이며 날짜 타입이 아님
//   BOOLEAN   — Oracle(23c 미만), MSSQL에서 DDL 에러
//   CLOB/BLOB — PostgreSQL, MSSQL에서 DDL 에러

// ──────────────────────────────────────────
// PostgreSQL 전용
// ──────────────────────────────────────────
const TYPE_POSTGRESQL = [
  // COMMON에서 제외된 공용 타입 (PG에서는 유효)
  "TEXT",                       // PG 가변길이 문자열 (제한 없음)
  "BIGINT",                     // PG 8byte 정수
  "TIMESTAMP",                  // PG 날짜+시간
  "BOOLEAN",                    // PG 네이티브 boolean
  // 정수 (PG 내부 alias, JDBC 반환값)
  "INT2",                       // = SMALLINT
  "INT4",                       // = INTEGER
  "INT8",                       // = BIGINT
  // 자동증가 (JDBC getColumnTypeName 반환)
  "SERIAL",                     // = INT4 + sequence
  "BIGSERIAL",                  // = INT8 + sequence
  "SMALLSERIAL",                // = INT2 + sequence
  // 문자열
  "BPCHAR",                     // = CHAR (PG 내부 이름, JDBC 반환)
  // 실수
  "FLOAT4",                     // = REAL
  "FLOAT8",                     // = DOUBLE PRECISION
  "DOUBLE PRECISION",           // FLOAT8의 정식 ANSI 이름
  // 통화
  "MONEY",                      // PG 통화 타입
  // 날짜·시간
  "TIMESTAMPTZ",                // = TIMESTAMP WITH TIME ZONE
  "INTERVAL",                   // 시간 간격
  // 바이너리
  "BYTEA",                      // PG 바이너리 (BLOB 대체)
  // 구조화·특수
  "JSON",
  "JSONB",                      // 인덱싱 가능 JSON
  "UUID",
] as const;

// ──────────────────────────────────────────
// Oracle 전용
// ──────────────────────────────────────────
const TYPE_ORACLE = [
  // COMMON에서 제외된 공용 타입 (Oracle에서 유효)
  "TIMESTAMP",                  // Oracle 날짜+시간+소수초
  // 문자열
  "VARCHAR2",                   // Oracle 권장 VARCHAR
  "NVARCHAR2",                  // 유니코드 VARCHAR
  "NCHAR",                      // 유니코드 고정길이
  "CLOB",                       // 대용량 텍스트
  "NCLOB",                      // 유니코드 CLOB
  "LONG",                       // deprecated, 레거시 호환
  // 숫자
  "NUMBER",                     // Oracle 통합 숫자 타입 NUMBER(p,s)
  "BINARY_FLOAT",               // 32bit IEEE 754
  "BINARY_DOUBLE",              // 64bit IEEE 754
  // 바이너리
  "BLOB",                       // Oracle 대용량 바이너리
  "RAW",                        // 가변길이 바이너리
  "LONG RAW",                   // deprecated, 레거시 호환
  // 날짜·시간
  "TIMESTAMP WITH TIME ZONE",
  "TIMESTAMP WITH LOCAL TIME ZONE",
] as const;

// ──────────────────────────────────────────
// MariaDB / MySQL 전용
// ──────────────────────────────────────────
const TYPE_MARIADB = [
  // COMMON에서 제외된 공용 타입 (MariaDB에서 유효)
  "TEXT",                       // MariaDB 가변길이 텍스트 (64KB)
  "BIGINT",                     // MariaDB 8byte 정수
  "TIMESTAMP",                  // MariaDB 날짜+시간 (UTC 변환)
  "BOOLEAN",                    // MariaDB → TINYINT(1) alias
  // 정수
  "TINYINT",                    // 1byte 정수
  "MEDIUMINT",                  // 3byte 정수
  // 실수
  "DOUBLE",                     // MySQL/MariaDB 전용 (PG: DOUBLE PRECISION)
  // 문자열
  "TINYTEXT",                   // 255byte
  "MEDIUMTEXT",                 // 16MB
  "LONGTEXT",                   // 4GB (CLOB 대체)
  // 날짜·시간
  "DATETIME",                   // DATE + TIME 결합
  "YEAR",                       // 연도만 저장
  // 논리·비트
  "BIT",                        // 비트 필드
  // 바이너리
  "BINARY",                     // 고정길이 바이너리
  "VARBINARY",                  // 가변길이 바이너리
  "BLOB",                       // 64KB 바이너리
  "TINYBLOB",                   // 255byte
  "MEDIUMBLOB",                 // 16MB
  "LONGBLOB",                   // 4GB
  // 구조화·특수
  "JSON",                       // MariaDB 10.2+
  "ENUM",                       // 열거형
  "SET",                        // 집합형
] as const;

// ──────────────────────────────────────────
// MSSQL (SQL Server) 전용
// ──────────────────────────────────────────
const TYPE_MSSQL = [
  // COMMON에서 제외된 공용 타입 (MSSQL에서 유효)
  "TEXT",                       // MSSQL deprecated (VARCHAR(MAX) 권장)
  "BIGINT",                     // MSSQL 8byte 정수
  // 문자열 (유니코드)
  "NVARCHAR",                   // 유니코드 VARCHAR
  "NCHAR",                      // 유니코드 고정길이
  "NTEXT",                      // deprecated, 레거시 호환
  // 정수
  "TINYINT",                    // 0~255 (unsigned)
  // 실수·통화
  "MONEY",                      // 8byte 통화
  "SMALLMONEY",                 // 4byte 통화
  // 날짜·시간 (MSSQL의 TIMESTAMP는 ROWVERSION이므로 여기 없음)
  "DATETIME",                   // 레거시 날짜시간
  "DATETIME2",                  // 고정밀 날짜시간 (TIMESTAMP 대체)
  "SMALLDATETIME",              // 분 단위 정밀도
  "DATETIMEOFFSET",             // TZ 포함 날짜시간
  // 논리
  "BIT",                        // MSSQL의 BOOLEAN 대체
  // 바이너리
  "BINARY",                     // 고정길이
  "VARBINARY",                  // 가변길이
  "IMAGE",                      // deprecated, 레거시 호환
  // 특수
  "UNIQUEIDENTIFIER",           // UUID 동등
  "XML",
] as const;